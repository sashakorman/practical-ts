// default increment
enum Direction {
    Up,
    Down
}

// auto increment
enum Direction1 {
    Up = 10,
    Down,
    Left,
    Right
}

// custom numbers
enum Direction2 {
    No = 9,
    Yes = 42
}

// string enums
enum Direction3 {
    Up = 'UP',
    Down = 'DOWN',
    Left = 'LEFT',
    Right = 'RIGHT'
}
